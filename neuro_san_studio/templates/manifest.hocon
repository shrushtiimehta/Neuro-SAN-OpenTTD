{
    # Empty. The upstream example templates and the agent network designer that wrote into
    # registries/generated/ were both removed from this fork; `ns init` scaffolds a project
    # with no starter network rather than one that cannot parse.
}
